
@inherits BaseButton


<Button Caption="@(Caption ?? "Edit")"
        Color="@(Color ?? "secondary")"
        Icon="@(Icon ?? "pencil")"
        OnClick="OnClick"
        ToolTipStr="@(ToolTipStr ?? "Edit")"
        IsOutline="@IsOutline"
        IsSmall="@IsSmall" />

