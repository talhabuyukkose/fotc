
@inherits BaseButton


<Button Caption="@(Caption ?? "Delete")"
        Color="@(Color ?? "danger")"
        Icon="@(Icon ?? "trash")"
        OnClick="OnClick"
        ToolTipStr="@(ToolTipStr ?? "Delete")"
        IsOutline="@IsOutline"
        IsSmall="@IsSmall" />

